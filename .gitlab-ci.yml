deploy:
  after_script:
  - python C:\Users\ASUS\Desktop\NCCU\Thesis\first-try\apps\updateDeployment.py -m
    0 -a "deploying"
  rules:
  - changes:
    - ASLFN/docker_apps/deployTmp
  script:
  - echo "deploy"
  - docker
  - cd ASLFN
  - docker build -t aslfn:latest -f rootuser.Dockerfile .
  - docker run -p 8070:8002 -d aslfn:latest
  - cd C:\Users\ASUS\Desktop\NCCU\Thesis\first-try\apps
  - docker ps -l | findstr aslfn > dockerTmp
  stage: deploy
  tags:
  - AILab
stages:
- deploy
